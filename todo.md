# todo

- [] Add s3 configuration for blob storage.
- [x] Configure PDS_RECOVERY_DID_KEY
- [] Wire `tests/run.sh` into a GitHub Actions workflow (Layer 1 on every push, Layer 2 nightly or on release tags).
- [] Add terms of service and privacy policy as files.